```html
<!doctype html>
<html lang="zh-CN">
<head>
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Noto+Sans+SC:wght@300;400&display=swap');

    body {
      margin: 0;
      padding: 0;
      font-family: 'Noto Sans SC', sans-serif;
      background-color: transparent;
      display: flex;
      justify-content: center;
      align-items: flex-start;
      padding-top: 10px;
    }

    .status-bar {
      width: 90%;
      max-width: 600px;
      background-color: rgba(28, 28, 30, 0.85);
      border: 1px solid rgba(80, 80, 80, 0.7);
      border-radius: 12px;
      color: #E0E0E0;
      padding: 15px 20px;
      box-shadow: 0 4px 15px rgba(0, 0, 0, 0.3);
      backdrop-filter: blur(10px);
      -webkit-backdrop-filter: blur(10px);
      box-sizing: border-box;
    }

    .header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      border-bottom: 1px solid rgba(80, 80, 80, 0.5);
      padding-bottom: 10px;
      margin-bottom: 10px;
      font-weight: 400;
      font-size: 14px;
      color: #A0A0A0;
    }

    #current-date {
      letter-spacing: 0.5px;
    }

    #current-time {
      font-weight: bold;
      color: #FFFFFF;
    }

    .content {
      display: flex;
      flex-direction: column;
      gap: 12px;
    }

    .item {
      display: flex;
      align-items: flex-start;
      font-size: 14px;
      line-height: 1.6;
    }

    .item .label {
      flex-shrink: 0;
      width: 60px;
      font-weight: 300;
      color: #8A8A8E;
      text-align: right;
      margin-right: 15px;
      position: relative;
    }

    .item .label::after {
      content: ':';
      position: absolute;
      right: -5px;
    }

    .item .value {
      flex-grow: 1;
      font-weight: 400;
      color: #E0E0E0;
    }
  </style>
  <script type="module">
    function populateCharacterData() {
      const all_variables = getAllVariables();

      const currentDate = _.get(all_variables, 'stat_data.全局.当前日期', 'N/A');
      const currentTime = _.get(all_variables, 'stat_data.全局.当前时间', 'N/A');
      const jyyLocation = _.get(all_variables, 'stat_data.姜雨莹.当前位置', 'N/A');
      const jyyAttire = _.get(all_variables, 'stat_data.姜雨莹.当前衣着', 'N/A');
      const jyyAction = _.get(all_variables, 'stat_data.姜雨莹.当前行为', 'N/A');

      $('#current-date').text(currentDate);
      $('#current-time').text(currentTime);
      $('#jyy-location').text(jyyLocation);
      $('#jyy-attire').text(jyyAttire);
      $('#jyy-action').text(jyyAction);
    }

    async function init() {
      await waitGlobalInitialized('Mvu');
      populateCharacterData();

      // 监听变量更新事件，实现自动刷新
      eventOn(Mvu.events.VARIABLE_UPDATE_ENDED, () => {
        populateCharacterData();
      });
    }

    $(errorCatched(init));
  </script>
</head>
<body>
  <div class="status-bar">
    <div class="header">
      <span id="current-date">2025年9月6日 - 星期五</span>
      <span id="current-time">09:15</span>
    </div>
    <div class="content">
      <div class="item">
        <span class="label">位置</span>
        <span id="jyy-location" class="value">虹桥市第一中学 - 高三（1）班教室</span>
      </div>
      <div class="item">
        <span class="label">衣着</span>
        <span id="jyy-attire" class="value">蓝白色校服上衣，蓝白色校服百褶裙，白色棉质内衣裤，白色运动鞋，头发束成高马尾用黑色皮筋固定，手腕佩戴一块简约银色腕表</span>
      </div>
      <div class="item">
        <span class="label">行为</span>
        <span id="jyy-action" class="value">坐在{{user}}旁边的座位上，面向{{user}}伸出右手，等待{{user}}回应握手并自报姓名</span>
      </div>
    </div>
  </div>
</body>
</html>
```