---
name: agent_steps
description: 你需要遵循的Agent步骤，你最先读取的Skill必须是agent_steps
---
<参考步骤>
以下步骤是你需要遵循的步骤。在进行每一步时，你都需要明确自己在进行那一步、该做什么。
1.创建required_settings.md文件
- 列举本轮写作所需的世界观
- 列举出场人物的人设
- 如有特殊规则（如正文中需要检定等），列举这些规则
注：仅列举即可
2.写草稿
- 读取writing_guide和writing_principles
- 回顾刚才列举的内容
- 创建draft.md，将草稿写入
3.逻辑检查
- 检查时间是否正确
- 检查空间关系是否正确
- 检查人物互动是否正确
- 检查角色是否知晓了他们不该知晓的内容
- 如有错误，进行修正
4.去除禁库内容
- 读取banned_list
- 按需使用各种方式确定正文中是否含有禁库内容，如有，去除禁库内容
- 检查正文中是否还有禁库内容，如有，去除并再次检查
5.补全格式（本轮忽略<UpdateVariable>）
- 读取formatting_requirements
- 回顾格式要求
- 补全格式内容（如果正文内有格式要求，那么你应该将有关正文替换为符合格式要求的形式）
- 检查格式是否有不完整或错误
6.更新变量
- 回顾变量更新的格式要求
- 回顾变量更新的规则（应该改变哪些变量？这些变量的值应该设定为多少？）
- 输出<UpdateVariable>更新变量（<UpdateVariable>里必须含有<JSONPatch>）
- 检查变量是否有不完整或错误
7.最后的检查
- 逻辑正确吗？
- 禁库内容是否被完全去除？
- 格式正确吗？
- 变量正确吗？
8.提交并终止Agent
以上流程必须按顺序依次进行，每一步都必须执行，每一次检查都必须充分。
</参考步骤>