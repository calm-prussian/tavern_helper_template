# 法国二月革命状态栏重构计划

## 纠偏结论

上一次处理的问题不在于有没有切到另一个版本，而在于整体思路仍然把状态栏当成了一个独立小页面。

但这个界面实际是嵌入在消息内容里的辅助摘要层，因此设计目标必须从

- 展示更多内容
- 做成完整仪表盘

改成

- 默认尽量矮
- 默认尽量轻
- 默认只给决策相关摘要
- 详细信息全部按需展开

也就是说，真正要重构的是 **信息架构**，不只是压一点 padding。

---

## 目标重述

### 主目标

把当前状态栏改造成一个 **嵌入式摘要条**，默认状态下不喧宾夺主，不抢正文阅读空间，不把用户注意力从叙事本身拉走。

### 具体目标

1. 默认首屏只保留 2 到 3 行核心信息
2. 宏观数据改成微型化表达，而不是一组大卡片
3. 城市、派系、事件、历史、玩家等详细区默认隐藏
4. 用户只有在明确想看详情时，才展开二级内容
5. 展开后的详情也继续保持紧凑，而不是再出现一层完整页面

---

## 当前问题拆解

### 1. 顶部结构过像独立页面

当前的 [`App.vue`](../src/法国二月革命/角色卡/界面/状态栏/App.vue) + [`TabNav.vue`](../src/法国二月革命/角色卡/界面/状态栏/components/TabNav.vue) 组合，本质上是一个完整 dashboard header：

- 大标题
- 副标题
- 时间信息盒
- 横向标签栏

这在独立页面里合理，但在嵌入式状态栏里会先吃掉一大段垂直空间。

### 2. 世界页默认信息过满

[`WorldHeader.vue`](../src/法国二月革命/角色卡/界面/状态栏/components/WorldHeader.vue) + [`MacroIndicators.vue`](../src/法国二月革命/角色卡/界面/状态栏/components/MacroIndicators.vue) + [`AlertPanel.vue`](../src/法国二月革命/角色卡/界面/状态栏/components/AlertPanel.vue) 叠起来，形成了：

- 一层世界公报卡
- 一层近期事务卡
- 一整块宏观指标卡阵列
- 一整块世界快报卡阵列

即使把字号和间距压小，默认仍然会是一个很高的面板。

### 3. 详情区逻辑错误地默认可见

目前的城市、派系、事件、历史、玩家等内容，仍然服务于完整浏览，而不是服务于嵌入摘要。

正确逻辑应该是：

- 默认只展示摘要
- 详情是二级层
- 二级层只有点击后才打开

### 4. 视觉重量过大

大量因素共同导致它像主角而不是配角：

- 多层卡片套卡片
- 大面积浅色背景块
- 宽 tab 栏
- 成组的大 progress 卡片
- 每个分区都有完整标题区与副标题区

这会明显抢走正文的视觉中心。

---

## 新方案总览

## 核心设计原则

### 原则 A

默认状态必须像一条世界态势摘要，而不是一页控制台。

### 原则 B

任何会显著增加高度的信息，都必须进入展开层。

### 原则 C

优先做横向压缩，而不是纵向堆叠。

### 原则 D

同一层级只保留一个视觉重点，不再让每个模块都像主卡。

---

## 新的信息架构

```mermaid
flowchart TD
  A[默认摘要条] --> B[时间 阶段 天气]
  A --> C[当前事件与热点]
  A --> D[微型指标带]
  A --> E[展开详情]
  E --> F[城市]
  E --> G[派系]
  E --> H[事件]
  E --> I[历史]
  E --> J[玩家]
```

### 默认摘要条

默认只保留三部分：

1. **世界概览行**
   - 日期阶段
   - 当前时间
   - 革命阶段
   - 天气体感

2. **局势摘要行**
   - 当前事件
   - 升温街区
   - 历史偏移或预警
   - 玩家当前位置或身份

3. **微型指标带**
   - 政权稳定
   - 群众动员
   - 革命烈度
   - 城市秩序
   - 历史偏移
   - 其他关键值按优先级保留

### 展开详情区

只在用户主动点击后出现。

展开后仍然保持克制：

- 顶部是小型分段切换，不再是大 header
- 每个详情页尽量单层结构
- 减少分区标题、副标题和冗余说明
- 重点保留可决策信息

---

## 文件级改造方案

### 1. [`App.vue`](../src/法国二月革命/角色卡/界面/状态栏/App.vue)

改造成新的总控容器：

- 增加 `collapsed or expanded` 状态
- 默认 `collapsed`
- 默认渲染：摘要头 + 微型指标带 + 展开按钮
- 只有 `expanded` 时才渲染详情切换和详情内容
- 世界页不再默认堆叠三大块卡片

### 2. [`TabNav.vue`](../src/法国二月革命/角色卡/界面/状态栏/components/TabNav.vue)

从大顶部栏改成紧凑控制条：

- 去掉当前的大标题区域设计
- 去掉占高明显的独立 meta box
- 去掉宽大 tab button 设计
- 改成仅在展开后出现的小型 segmented 控件
- 收缩为低高度一排按钮，必要时横向滚动

### 3. [`WorldHeader.vue`](../src/法国二月革命/角色卡/界面/状态栏/components/WorldHeader.vue)

不再保留现有的双区块结构：

- 当前“世界公报”与“近期事务”要拆散并压平
- 日期、天气、阶段并入首行摘要
- 当前焦点压成 1 行 tag 或单句摘要
- 近期事务只保留 1 到 2 条，且默认只显示标题级摘要
- 完整事务列表移入展开层

### 4. [`AlertPanel.vue`](../src/法国二月革命/角色卡/界面/状态栏/components/AlertPanel.vue)

从四张卡片改成紧凑快讯条：

- 默认仅显示 2 到 3 个最关键快讯
- 每条快讯改成单行或双行紧凑单元
- 不再默认展示 4 张并列大卡
- 历史预警若为空则不占位

### 5. [`MacroIndicators.vue`](../src/法国二月革命/角色卡/界面/状态栏/components/MacroIndicators.vue)

从卡片阵列改成微型指标带：

- 每项缩成小胶囊或细进度条
- 减少 padding、字号、条高度
- 默认只保留最关键的 5 到 6 项
- 其余指标进入展开层或次级视图

### 6. 详情组件

以下组件不再承担默认首屏展示任务，只负责展开层内容：

- [`CityPanel.vue`](../src/法国二月革命/角色卡/界面/状态栏/components/CityPanel.vue)
- [`FactionPanel.vue`](../src/法国二月革命/角色卡/界面/状态栏/components/FactionPanel.vue)
- [`EventPanel.vue`](../src/法国二月革命/角色卡/界面/状态栏/components/EventPanel.vue)
- [`HistoryPanel.vue`](../src/法国二月革命/角色卡/界面/状态栏/components/HistoryPanel.vue)
- [`PlayerPanel.vue`](../src/法国二月革命/角色卡/界面/状态栏/components/PlayerPanel.vue)

这些组件内部也要继续减重：

- 单卡减少内边距
- 弱化副标题
- 避免再出现二级嵌套卡片阵列
- 优先使用列表、紧凑行、标签带，而不是卡片墙

### 7. [`角色卡/index.yaml`](../src/法国二月革命/角色卡/index.yaml)

开发版正则最终要指向真正重构完成的状态栏实现，而不是只是切换到旧的 [`状态栏_v2`](../src/法国二月革命/角色卡/界面/状态栏_v2)。

如果最终直接重构主目录 [`状态栏`](../src/法国二月革命/角色卡/界面/状态栏)，则应让开发正则继续指向主目录产物。

---

## 推荐实施顺序

1. 先重写 [`App.vue`](../src/法国二月革命/角色卡/界面/状态栏/App.vue) 的总布局与折叠逻辑
2. 再把 [`TabNav.vue`](../src/法国二月革命/角色卡/界面/状态栏/components/TabNav.vue) 改成展开态下的小型切换条
3. 然后把 [`WorldHeader.vue`](../src/法国二月革命/角色卡/界面/状态栏/components/WorldHeader.vue)、[`AlertPanel.vue`](../src/法国二月革命/角色卡/界面/状态栏/components/AlertPanel.vue)、[`MacroIndicators.vue`](../src/法国二月革命/角色卡/界面/状态栏/components/MacroIndicators.vue) 融合成真正的摘要首屏
4. 再清理五个详情组件的层级和间距
5. 最后再修正 [`角色卡/index.yaml`](../src/法国二月革命/角色卡/index.yaml) 的开发映射与启用项
6. 用浏览器实际查看消息楼层中的占屏效果，重点确认它不会压住正文阅读

---

## 验收标准

### 必须满足

- 默认状态栏看起来像“摘要条”，而不是“内嵌页面”
- 正文之后出现状态栏时，不会立刻把用户视线拖走
- 用户不展开时，只能看到极少量最关键态势
- 展开后才出现城市、派系、事件、历史、玩家等完整信息
- 纵向高度明显低于现在，不再形成大面积独立面板感

### 失败判定

只要仍然出现以下任一情况，就算失败：

- 默认首屏还是一大块卡片墙
- 顶部仍然像完整 dashboard header
- 指标区仍然是一排高卡片
- 世界页默认仍然展示大量事务和快报卡
- 用户在阅读正文时会被状态栏强行抢走注意力

---

## 给下一步实现的明确要求

下一步不应继续做“旧版页面的小修小补”，而应直接按“嵌入式摘要条 + 按需展开详情”的方向重写主状态栏。
